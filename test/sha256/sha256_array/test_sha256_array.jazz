from XMSS require "sha256/sha256_array/sha256_array.jtmpl"

export fn sha256_array<INLEN>(reg ptr u8[32] out, reg ptr u8[INLEN] _in) -> reg ptr u8[32]
{
  stack  u8[INLEN] in;
  inline int i;

  for i=0 to INLEN { in[i] = _in[i]; }

  out = __sha256<INLEN>(out, in); 
  return out;
}//<>
